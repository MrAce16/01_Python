# if target is found, return the index of the target and if target not found return -1
def liear_search(arr,target):
    for i in range
    


arr = [10, 20, 30, 40, 50]
target = 30
print(linear_search(arr, target))